git add and commit

1、修改不add 文件图标和文件名之间有一个>符号，代表修改无add，无commit.
2、修改后add 文件图标出现黑色的×。
3、修改后，不add,和本地代码库进行比较。发现本地的代码可以和许多地方进行比较：
右键项目或者文件>compare with>local history(本地工作空间中版本，保存一次算一次比较)
						   >index        (add 后的本地缓存区)
						   >commit       (提交到本地的版本)
4、本地修改后，撤销修改。还原之前提交后的代码。